{
  "name": "Zepp OS screen reader",
  "version": "1.0.0",
  "description": "this is screen reader for Zepp OS",
  "main": "app.js",
  "scripts": {
		"dev": "zeus dev",
		"build": "zeus build",
		"preview": "zeus preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NVC DevelopmentTeam",
  "license": "ISC",
  "devDependencies": {
    "@zeppos/device-types": "^3.0.0"
  }
}
